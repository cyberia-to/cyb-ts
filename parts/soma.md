---
title: soma
tags: cyb, core
alias: cyb soma
crystal-type: entity
crystal-domain: cyb
---
# soma

mind and model: local inference on the robot's own silicon. live today — the soma kernel runs over honeycrisp, answers `? question` in [[cyb/parts/com|com]], casts the exchange into the graph so [[cyb/parts/brain|brain]] renders what was thought. nobody reads your questions: the weights are yours, the compute is yours.

part of the [[cyb/anatomy|anatomy]] (mind). the model is a hash of the graph in the limit — see [[ct0]] for the compilation path
